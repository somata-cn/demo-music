{
  "name": "netease-music-pages",
  "version": "1.0.0",
  "description": "Netease music resolver for Cloudflare Pages Functions",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --port 9001",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
